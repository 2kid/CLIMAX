@model IEnumerable<CLIMAX.Models.RoleType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.roleType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.roleType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoleTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoleTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoleTypeId })
        </td>
    </tr>
}

</table>
